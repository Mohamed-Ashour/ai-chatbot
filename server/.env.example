# FastAPI Server Environment Configuration

# Application Environment
# Set to 'development' for local development or 'production' for deployment
APP_ENV=development

# Server Configuration
HOST=0.0.0.0
PORT=3500

# Redis Configuration
# Replace with your Redis instance details
REDIS_URL=localhost:6379
REDIS_USER=
REDIS_PASSWORD=

# Session Configuration
# Session timeout in seconds (3600 = 1 hour)
SESSION_TIMEOUT=3600

# CORS Configuration (automatically set based on APP_ENV)
# Development: allows all origins
# Production: restricts to specific origins
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Logging
LOG_LEVEL=INFO

# Optional: Custom Redis database number
# REDIS_DB=0